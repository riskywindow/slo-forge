# Helix resume bullets

- Built a deterministic, evidence-carrying online-learning reference architecture spanning
  coordinated model/environment/effect capture, counterfactual branching, policy-versioned rollout,
  reward integrity, provenance-complete training, gated promotion, and rollback.
- Designed strict, bounded, hash-addressed Python/Rust IR contracts that fail closed on policy mixing,
  stale evidence, incompatible state reuse, policy-visible verifier leakage in the checked dataflow,
  artifact tampering, and illegal effects.
- Implemented a serving-hard learning-aware resource compiler with baseline policies, capacity lending,
  bounded preemption/preservation, fault injection, complete audit decisions, and raw-evidence provenance.
- Added tenant/privacy/consent/retention controls, selective experience curation, transactional champion
  routing, active-session compatibility, deterministic CPU evaluation, and a 16-fault campaign matrix.

Use these only with the scope qualifier: local deterministic CPU reference implementation; no claim of
production learning gains, GPU performance, distributed consensus, or autonomous deployment.
